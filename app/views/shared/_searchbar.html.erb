  <div class="container-home-form">
    <div class="home-form">
      <%= simple_form_for :request, url: programs_path, method: :get do |f| %>
        <div class="home-form-text-field">
        <%= f.input :description, placeholder: "Peppa Pig", label: false, required: false %>
        </div>
        <div class="home-form-categ-field">
        <%= f.input :category, collection: Program::CATEGORY, prompt: "Catégorie", label: false, required: false %>
        </div>
        <div class="home-form-age-field">
        <%= f.input :min_age, label: false, placeholder: "À partir de quel âge ?", as: :numeric, input_html: { min: 0 }, required: false %>
        </div>
        <div class="home-form-length-field">
        <%= f.input :length, collection: Program::LENGTH, prompt: "Durée", label: false, required: false %>
        </div>
        <div class="home-form-submit-btn">
        <%= f.button :submit, wrapper_html: { class: 'fas fa-search' }, label: false, class: "btn-search" %>
        </div>
      <% end %>
    </div>
  </div>
